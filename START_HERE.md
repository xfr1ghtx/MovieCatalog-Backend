# 🚀 Начните здесь!

## Добро пожаловать в MovieCatalog Backend!

Этот файл поможет вам быстро начать работу с проектом.

---

## ⚡ Быстрый старт (5 минут)

### 1️⃣ Запустите проект
```bash
# Запустить все сервисы (API, PostgreSQL, pgAdmin)
docker-compose up -d

# Дождитесь запуска (проверьте статус)
docker-compose ps
```

### 2️⃣ Настройте базу данных
```bash
# Примените миграции
docker-compose exec api alembic upgrade head

# Загрузите тестовые данные (8 фильмов, 10 жанров)
docker-compose exec api python -m app.seed
```

### 3️⃣ Откройте Swagger UI
Перейдите в браузере: **http://localhost:8000/swagger**

### 4️⃣ Зарегистрируйте пользователя
В Swagger UI найдите `POST /api/account/register` и выполните:
```json
{
  "userName": "demo",
  "name": "Demo User",
  "password": "demo123",
  "email": "demo@example.com",
  "gender": 0
}
```

### 5️⃣ Авторизуйтесь
1. Скопируйте полученный `token`
2. Нажмите кнопку **"Authorize"** в правом верхнем углу
3. Вставьте токен (БЕЗ "Bearer")
4. Нажмите **"Authorize"**

### 6️⃣ Протестируйте API
Теперь вы можете использовать все endpoints! Попробуйте:
- `GET /api/movies/1` - Список фильмов
- `GET /api/account/profile` - Ваш профиль
- `POST /api/favorites/{id}/add` - Добавить в избранное

---

## 📚 Документация

### Для новичков
1. **[QUICKSTART.md](QUICKSTART.md)** - Пошаговое руководство
2. **[API_ENDPOINTS.md](API_ENDPOINTS.md)** - Справочник по всем endpoints

### Для разработчиков
3. **[README.md](README.md)** - Полная документация
4. **[ARCHITECTURE.md](ARCHITECTURE.md)** - Архитектура проекта
5. **[PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)** - Итоговая информация

### Для изучения кода
- `app/api/routes/` - Начните отсюда, чтобы понять endpoints
- `app/services/` - Бизнес-логика
- `app/models/` - Структура базы данных

---

## 🎯 Что создано?

✅ **17 API endpoints** для работы с:
- Аутентификацией (регистрация, вход, выход)
- Профилем пользователя
- Каталогом фильмов
- Избранными фильмами
- Отзывами на фильмы

✅ **7 таблиц БД** в PostgreSQL:
- users, movies, genres, reviews, favorites, tokens

✅ **JWT аутентификация**:
- Access + Refresh токены
- Bcrypt хеширование паролей

✅ **Docker окружение**:
- FastAPI приложение (порт 8000)
- PostgreSQL 15 (порт 5432)
- pgAdmin 4 (порт 5050)

---

## 🔗 Полезные ссылки

После запуска проекта:
- **Swagger UI**: http://localhost:8000/swagger
- **ReDoc**: http://localhost:8000/redoc
- **pgAdmin**: http://localhost:5050 (admin@admin.com / admin)
- **Health Check**: http://localhost:8000/health

---

## 🆘 Помощь

### Проблемы с запуском?
```bash
# Посмотрите логи
docker-compose logs -f

# Пересоздайте контейнеры
docker-compose down
docker-compose up -d
```

### Порты заняты?
Отредактируйте `docker-compose.yml` и измените порты:
```yaml
ports:
  - "8001:8000"  # Вместо 8000:8000
```

### База данных не работает?
```bash
# Остановите все
docker-compose down -v

# Запустите заново
docker-compose up -d
docker-compose exec api alembic upgrade head
docker-compose exec api python -m app.seed
```

---

## 📊 Статистика проекта

- **53 файла** создано
- **39 Python файлов**
- **~2500+ строк кода**
- **Архитектура**: Clean Architecture с разделением на слои

---

## 🎓 Следующие шаги

### Для изучения:
1. Откройте Swagger и изучите все endpoints
2. Посмотрите код в `app/api/routes/auth.py`
3. Изучите модели в `app/models/`
4. Прочитайте ARCHITECTURE.md

### Для разработки:
1. Добавьте свои endpoints
2. Расширьте модели данных
3. Добавьте новые фичи (поиск, фильтры, и т.д.)

### Для production:
1. Смените `JWT_SECRET_KEY` в `.env`
2. Настройте HTTPS
3. Добавьте rate limiting
4. Настройте мониторинг

---

## 🌟 Возможности

Что можно сделать с этим API:

### Пользователи могут:
- ✅ Регистрироваться и входить в систему
- ✅ Управлять своим профилем
- ✅ Просматривать каталог фильмов
- ✅ Добавлять фильмы в избранное
- ✅ Оставлять отзывы (публичные или анонимные)
- ✅ Редактировать и удалять свои отзывы

### API предоставляет:
- ✅ JWT аутентификацию
- ✅ Пагинацию списков
- ✅ Валидацию данных
- ✅ Обработку ошибок
- ✅ Swagger документацию

---

## 💡 Примеры использования

### Зарегистрироваться
```bash
curl -X POST "http://localhost:8000/api/account/register" \
  -H "Content-Type: application/json" \
  -d '{
    "userName": "john",
    "name": "John Doe",
    "password": "secret123",
    "email": "john@test.com",
    "gender": 0
  }'
```

### Получить список фильмов
```bash
curl "http://localhost:8000/api/movies/1"
```

### Добавить отзыв
```bash
curl -X POST "http://localhost:8000/api/movie/{MOVIE_ID}/review/add" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "reviewText": "Отличный фильм!",
    "rating": 10,
    "isAnonymous": false
  }'
```

---

## 🎉 Готово!

Проект полностью настроен и готов к использованию!

**Следующий шаг**: Откройте **http://localhost:8000/swagger** и начните экспериментировать!

Хорошего изучения! 🚀

---

**P.S.** Если что-то непонятно - смотрите документацию в файлах проекта или изучайте код в `app/` директории. Код хорошо структурирован и закомментирован.

